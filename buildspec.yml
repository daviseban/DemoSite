version: 0.2
phases:
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn install
      - echo "******** Uploading to S3 ********"
artifacts:
  files:
    - site/target/mycompany.war
    - admin/target/admin.war
  discard-paths: yes
